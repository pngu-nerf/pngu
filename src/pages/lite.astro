---
/* lite.astro - Pure Performance / Zero-JS */
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>PNGU | Lite</title>
		<style is:inline>
			/* The "One-Packet" CSS - Inlined for 0ms network overhead */
			:root {
				--bg: #333333;
				--text: #ffffff;
				--accent: #00d1ff; /* A bright cyan for contrast links */
				--border: #444444;
			}
			
			* { box-sizing: border-box; margin: 0; padding: 0; }
			
			body {
				background-color: var(--bg);
				color: var(--text);
				/* System font stack = 0KB download */
				font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
				line-height: 1.6;
				max-width: 650px;
				margin: 0 auto;
				padding: 2rem 1rem;
			}

			header { text-align: center; margin-bottom: 2.5rem; }
			
			/* Decoding async prevents the image from blocking the text render */
			img { 
				max-width: 100%; 
				height: auto; 
				display: block; 
				margin: 0 auto; 
			}

			a { color: var(--accent); text-decoration: none; }
			a:hover { text-decoration: underline; }

			.content-box {
				border: 1px solid var(--border);
				padding: 1.5rem;
				margin-bottom: 1.25rem;
				border-radius: 4px;
			}

			h1, h2 { margin-bottom: 0.75rem; font-weight: 600; }
			ul { list-style: none; }
			li { margin-bottom: 0.5rem; }

			.meta { opacity: 0.6; font-size: 0.9rem; margin-top: 2rem; text-align: center; }
		</style>
	</head>
	<body>
		<header>
			<img 
				src="/site-assets/banner.png" 
				alt="PNGU Logo" 
				width="200" 
				decoding="async"
			/>
		</header>

		<main>
			<section class="content-box">
				<h1>Simplified Version</h1>
				<p>This is a low-bandwidth version of pngu.info. No animations, no scripts, no tracking.</p>
			</section>

			<section class="content-box">
				<h2>Navigation</h2>
				<ul>
					<li><a href="https://pngu.info">Main Site (Cinematic)</a></li>
					<li><a href="/projects">Project Directory</a></li>
					<li><a href="/contact">Direct Contact</a></li>
				</ul>
			</section>

			<section class="content-box">
				<h2>Status</h2>
				<p>All systems operational. Performance-first build active.</p>
			</section>
		</main>

		<footer class="meta">
			<p>&copy; {new Date().getFullYear()} PNGU — Lite Mode</p>
		</footer>
	</body>
</html>
