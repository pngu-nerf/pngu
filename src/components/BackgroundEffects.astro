---
// src/components/BackgroundEffects.astro
---
<div id="bg-container">
    <canvas id="tech-canvas"></canvas>
    <div id="glow-overlay"></div>
</div>

<style>
    #bg-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        pointer-events: none;
    }

    #tech-canvas {
        position: absolute;
        width: 100%;
        height: 100%;
        
        /* Blur Effect */
        filter: blur(30px);

        /* Initial State */
        opacity: 0.4;
        
        /* Animation */
        animation: brightenUp 2s ease-out forwards;
        will-change: opacity;
    }

    /* New Glow Overlay */
    #glow-overlay {
        position: absolute;
        inset: 0;
        /* Linear gradient: White glow at top (0%), transparent middle, white glow at bottom (100%) */
        background: linear-gradient(
            to bottom, 
            rgba(255, 255, 255, 0.08) 0%, 
            transparent 25%, 
            transparent 75%, 
            rgba(255, 255, 255, 0.08) 100%
        );
        /* Slight blur to make the glow super soft */
        backdrop-filter: blur(5px);
    }

    @keyframes brightenUp {
        from { opacity: 0.4; }
        to { opacity: 0.8; } 
    }
</style>

<script>
    const canvas = document.getElementById('tech-canvas') as HTMLCanvasElement;
    const ctx = canvas.getContext('2d');

    let width, height;
    let strands = [];
    
    // EXTENSION: How far past the screen (top/bottom) strands extend
    const EXTENSION = 300; 

    let mouseX = -1000;
    let mouseSpeedX = 0;

    window.addEventListener('mousemove', (e) => {
        mouseSpeedX = e.movementX;
        mouseX = e.clientX;
    });

    const STRAND_COLOR = '255, 255, 255'; 

    class Strand {
        x: number;
        length: number;
        thickness: number;
        offset: number;
        amplitude: number;
        frequency: number;
        depth: number;
        strumVal: number; 
        strumVel: number; 
        highlight: number;
        
        alphaMult: number; 
        drawnDepth: number;

        constructor(index, totalStrands, layerCfg) {
            this.init(index, totalStrands, layerCfg);
        }

        init(index, totalStrands, layerCfg) {
            const slotWidth = width / totalStrands;
            const slotStart = index * slotWidth;
            this.x = slotStart + (Math.random() * (slotWidth * 0.8)) + (slotWidth * 0.1);

            // GEOMETRY UPDATE: Length is screen height + top extension + bottom extension
            this.length = height + (EXTENSION * 2); 
            
            this.depth = Math.random();
            this.drawnDepth = this.depth + layerCfg.zOffset;
            this.alphaMult = layerCfg.alphaMult;

            const baseThickness = (0.5 + (this.depth * 4)) * 10;
            this.thickness = baseThickness * layerCfg.thickMult;
            
            this.offset = Math.random() * 100; 
            this.amplitude = Math.random() * 50 + 20; 
            this.frequency = Math.random() * 0.002 + 0.001; 
            
            this.strumVal = 0;
            this.strumVel = 0;
            this.highlight = 0;
        }

        update() {
            this.offset += 0.005;

            const tension = 0.5; 
            const damping = 0.2; 
            
            const force = -tension * this.strumVal;
            this.strumVel += force;
            this.strumVel *= damping; 
            this.strumVal += this.strumVel;

            this.highlight *= 0.9; 

            const dist = Math.abs(mouseX - this.x);
            
            if (dist < (150 * this.alphaMult)) { 
                const depthImpact = 0.1 + (this.depth * 0.9);
                this.strumVel += (mouseSpeedX * 0.02) * depthImpact;
                this.highlight = Math.min(this.highlight + 0.1 + Math.abs(mouseSpeedX * 0.005), 1.0);
            }
        }

        draw() {
            if (!ctx) return;
            
            ctx.beginPath();
            
            const baseMaxAlpha = 0.2 + (this.depth * 0.8);
            const finalMaxAlpha = baseMaxAlpha * this.alphaMult;

            // GRADIENT UPDATE: Covers the full extended height
            // -EXTENSION (Top of strand) to height + EXTENSION (Bottom of strand)
            const grad = ctx.createLinearGradient(this.x, -EXTENSION, this.x, height + EXTENSION);
            
            // Solid color mostly, fading only at the extreme off-screen tips
            grad.addColorStop(0, `rgba(${STRAND_COLOR}, 0)`);       // Off-screen top
            grad.addColorStop(0.1, `rgba(${STRAND_COLOR}, ${finalMaxAlpha})`); // Visible top
            grad.addColorStop(0.9, `rgba(${STRAND_COLOR}, ${finalMaxAlpha})`); // Visible bottom
            grad.addColorStop(1, `rgba(${STRAND_COLOR}, 0)`);       // Off-screen bottom

            ctx.strokeStyle = grad;
            
            ctx.lineWidth = this.thickness + (this.highlight * 2);
            ctx.lineCap = 'round';
            
            const segments = 25;
            const segmentHeight = this.length / segments;

            // DRAW LOOP UPDATE: Start drawing from -EXTENSION (off-screen top)
            const startY = -EXTENSION;

            ctx.moveTo(
                this.x + Math.sin(this.offset) * this.amplitude, 
                startY
            );

            for (let i = 0; i <= segments; i++) {
                // Calculate Y relative to the extended start point
                let currentY = startY + (segmentHeight * i);
                
                const ambientSway = Math.sin(this.offset + (i * 0.2)) * this.amplitude;
                const strumEffect = this.strumVal * Math.sin((i / segments) * Math.PI);
                
                ctx.lineTo(this.x + ambientSway + strumEffect, currentY);
            }

            ctx.shadowBlur = (5 + (this.highlight * 20)) * this.depth;
            ctx.shadowColor = `rgba(255, 255, 255, ${(0.5 + this.highlight * 0.5) * this.alphaMult})`;

            ctx.stroke();
            ctx.shadowBlur = 0; 
        }
    }

    function resize() {
        width = window.innerWidth;
        height = window.innerHeight;
        canvas.width = width;
        canvas.height = height;
        
        strands = [];
        const BASE_COUNT = Math.floor(Math.random() * 2) + 5;

        // Layer Config
        const layers = [
            { countMult: 2.25, thickMult: 0.25, alphaMult: 0.2, zOffset: -2 },
            { countMult: 1.5,  thickMult: 0.5,  alphaMult: 0.4, zOffset: -1 },
            { countMult: 1.0,  thickMult: 1.0,  alphaMult: 0.8, zOffset: 0  }
        ];

        layers.forEach(layerCfg => {
            const layerCount = Math.round(BASE_COUNT * layerCfg.countMult);
            for (let i = 0; i < layerCount; i++) {
                strands.push(new Strand(i, layerCount, layerCfg));
            }
        });
    }

    function animate() {
        if (!ctx) return;
        ctx.clearRect(0, 0, width, height);

        strands.sort((a, b) => a.drawnDepth - b.drawnDepth);

        strands.forEach(strand => {
            strand.update();
            strand.draw();
        });
        
        mouseSpeedX = 0;

        requestAnimationFrame(animate);
    }

    window.addEventListener('resize', resize);
    resize();
    animate();
</script>
