---
// src/components/BackgroundEffects.astro
---
<canvas id="tech-canvas"></canvas>

<style>
    #tech-canvas {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        pointer-events: none;
        
        /* Blur Effect */
        filter: blur(30px);

        /* Initial State: 50% Opacity */
        opacity: 0.5;
        
        /* Animation: Smoothly brighten to 100% */
        animation: brightenUp 2s ease-out forwards;

        /* Mobile Optimization */
        will-change: opacity;
    }

    @keyframes brightenUp {
        from { opacity: 0.5; }
        to { opacity: 1; } 
    }
</style>

<script>
    const canvas = document.getElementById('tech-canvas') as HTMLCanvasElement;
    const ctx = canvas.getContext('2d');

    let width, height;
    let strands = [];
    
    let mouseX = -1000;
    let mouseSpeedX = 0;

    window.addEventListener('mousemove', (e) => {
        mouseSpeedX = e.movementX;
        mouseX = e.clientX;
    });

    // Randomly choose 5 or 6 strands
    const STRAND_COUNT = Math.floor(Math.random() * 2) + 5;
    const STRAND_COLOR = '255, 255, 255'; 

    class Strand {
        x: number;
        length: number;
        thickness: number;
        offset: number;
        amplitude: number;
        frequency: number;
        depth: number;
        strumVal: number; 
        strumVel: number; 
        highlight: number; 

        // New: Accept index and total count to calculate position slot
        constructor(index, totalStrands) {
            this.init(index, totalStrands);
        }

        init(index, totalStrands) {
            // SPACING LOGIC:
            // Divide screen into equal slots (e.g., 5 slots if 5 strands)
            // Place strand randomly strictly within its slot.
            const slotWidth = width / totalStrands;
            const slotStart = index * slotWidth;
            
            // Add random padding so it's not right on the edge of the slot
            this.x = slotStart + (Math.random() * (slotWidth * 0.8)) + (slotWidth * 0.1);

            this.length = height; 
            
            this.depth = Math.random();

            // Thickness (5px to 45px)
            this.thickness = (0.5 + (this.depth * 4)) * 10;
            
            this.offset = Math.random() * 100; 
            this.amplitude = Math.random() * 50 + 20; 
            this.frequency = Math.random() * 0.002 + 0.001; 
            
            this.strumVal = 0;
            this.strumVel = 0;
            this.highlight = 0;
        }

        update() {
            this.offset += 0.005;

            const tension = 0.5; 
            const damping = 0.2; 
            
            const force = -tension * this.strumVal;
            this.strumVel += force;
            this.strumVel *= damping; 
            this.strumVal += this.strumVel;

            this.highlight *= 0.9; 

            const dist = Math.abs(mouseX - this.x);
            
            if (dist < 150) { 
                const depthImpact = 0.1 + (this.depth * 0.9);
                this.strumVel += (mouseSpeedX * 0.02) * depthImpact;
                this.highlight = Math.min(this.highlight + 0.1 + Math.abs(mouseSpeedX * 0.005), 1.0);
            }
        }

        draw() {
            if (!ctx) return;
            
            ctx.beginPath();
            
            const maxAlpha = 0.2 + (this.depth * 0.8);

            const grad = ctx.createLinearGradient(this.x, 0, this.x, height);
            
            // FADE LOGIC:
            // 0% (Top): Transparent
            grad.addColorStop(0, `rgba(${STRAND_COLOR}, 0)`);
            
            // 20% down: Full Opacity
            grad.addColorStop(0.2, `rgba(${STRAND_COLOR}, ${maxAlpha})`); 
            
            // 100% (Bottom): Still Full Opacity (No fade out)
            grad.addColorStop(1, `rgba(${STRAND_COLOR}, ${maxAlpha})`);

            ctx.strokeStyle = grad;
            
            ctx.lineWidth = this.thickness + (this.highlight * 2);
            ctx.lineCap = 'round';
            
            const segments = 25;
            const segmentHeight = this.length / segments;

            ctx.moveTo(
                this.x + Math.sin(this.offset) * this.amplitude, 
                0
            );

            for (let i = 0; i <= segments; i++) {
                let currentY = segmentHeight * i;
                
                const ambientSway = Math.sin(this.offset + (i * 0.2)) * this.amplitude;
                const strumEffect = this.strumVal * Math.sin((i / segments) * Math.PI);
                
                ctx.lineTo(this.x + ambientSway + strumEffect, currentY);
            }

            ctx.shadowBlur = (5 + (this.highlight * 20)) * this.depth;
            ctx.shadowColor = `rgba(255, 255, 255, ${0.5 + this.highlight * 0.5})`;

            ctx.stroke();
            ctx.shadowBlur = 0; 
        }
    }

    function resize() {
        width = window.innerWidth;
        height = window.innerHeight;
        canvas.width = width;
        canvas.height = height;
        
        strands = [];
        // Pass index 'i' and total count to constructor
        for (let i = 0; i < STRAND_COUNT; i++) {
            strands.push(new Strand(i, STRAND_COUNT));
        }
    }

    function animate() {
        if (!ctx) return;
        ctx.clearRect(0, 0, width, height);

        // Sort strands by depth for layering
        strands.sort((a, b) => a.depth - b.depth);

        strands.forEach(strand => {
            strand.update();
            strand.draw();
        });
        
        mouseSpeedX = 0;

        requestAnimationFrame(animate);
    }

    window.addEventListener('resize', resize);
    resize();
    animate();
</script>
