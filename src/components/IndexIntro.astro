---
/* IndexIntro.astro - Persistent Logo & Instant Sync Reveal */
---

<div id="intro-blackout"></div>
<div id="wipe-layer"></div>
<div id="intro-banner-overlay"></div>

<style is:global>
  body.is-intro {
    overflow: hidden !important;
    height: 100vh !important;
    position: fixed;
    width: 100%;
    top: 0;
    left: 0;
  }

  /* 1. INITIAL STATE: Real UI is hidden but the header is ready */
  body.is-intro main, 
  body.is-intro .index-header,
  body.is-intro .content-box,
  body.is-intro .separator,
  body.is-intro .separator-small {
    visibility: hidden !important;
    opacity: 0 !important;
  }

  /* We keep the vertical tuck very shallow for maximum speed */
  body.is-intro .content-box,
  body.is-intro .separator {
    transform: translateY(8px);
  }

  /* 2. PERSISTENT BANNER OVERLAY (No Shadow) */
  #intro-banner-overlay {
    position: fixed;
    z-index: 10001; 
    pointer-events: none;
    display: none;
    /* Visible from the start */
    opacity: 1; 
    will-change: opacity;
  }

  #intro-banner-overlay img {
    width: 100%;
    height: auto;
    display: block;
  }

  /* 3. CURTAIN LAYERS */
  #intro-blackout {
    position: fixed;
    inset: 0;
    background: #000;
    z-index: 10000;
  }

  #wipe-layer {
    position: fixed;
    inset: 0;
    height: 400vh;
    z-index: 9999;
    background: linear-gradient(to bottom, #000 0%, #000 75%, transparent 100%);
    transform: translateY(0); 
    pointer-events: none;
  }

  /* 4. ANIMATION - High-Tension Cubic Bezier */
  .animate-wipe {
    transition: transform 0.9s cubic-bezier(0.19, 1, 0.22, 1);
  }

  .reveal-static {
    visibility: visible !important;
    opacity: 1 !important;
    transition: opacity 0.1s linear !important;
  }

  .reveal-active {
    visibility: visible !important;
    opacity: 1 !important;
    transform: translateY(0) !important;
    /* Ultrfast 0.2s reveal */
    transition: 
      opacity 0.2s ease-out, 
      transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.2) !important;
  }
</style>

<script>
  function runIntro() {
    const internalReferrer = document.referrer && document.referrer.includes(window.location.hostname);
    const blackout = document.getElementById('intro-blackout');
    const wipe = document.getElementById('wipe-layer');
    const overlay = document.getElementById('intro-banner-overlay');
    const actualBanner = document.querySelector('.banner-image') as HTMLImageElement;

    if (internalReferrer) {
      blackout?.remove();
      wipe?.remove();
      overlay?.remove();
      document.body.classList.remove('is-intro');
      return; 
    }

    if (!actualBanner || !overlay) return;

    // PIN OVERLAY IMMEDIATELY
    const rect = actualBanner.getBoundingClientRect();
    overlay.style.width = `${rect.width}px`;
    overlay.style.top = `${rect.top}px`;
    overlay.style.left = `${rect.left}px`;
    overlay.innerHTML = `<img src="${actualBanner.src}" alt="Overlay Banner">`;
    overlay.style.display = 'block';
    overlay.style.opacity = '1';

    window.scrollTo(0, 0);
    document.body.classList.add('is-intro');

    // 1. THE 2s HOLD (The Blackout is active, but Logo is already visible)
    setTimeout(() => {
      if (blackout) blackout.style.display = 'none';
      
      if (wipe) {
        wipe.classList.add('animate-wipe');
        wipe.style.transform = 'translateY(-100%)';
      }

      // 2. INSTANT SYNC REVEAL
      // Fired at 0ms delay relative to the wipe starting
      const header = document.querySelector('.index-header');
      if (header) header.classList.add('reveal-static');

      const uiElements = document.querySelectorAll('.content-box, .separator, .separator-small');
      uiElements.forEach((el, index) => {
        setTimeout(() => {
          el.classList.add('reveal-active');
        }, index * 30); // Ultra-tight 30ms stagger
      });

      // 3. CLEANUP HANDOFF
      setTimeout(() => {
        document.body.classList.remove('is-intro');
        // Smoothly exit the proxy logo now that the real one is under it
        overlay.style.opacity = '0';
        overlay.style.transition = 'opacity 0.3s ease-in-out';

        setTimeout(() => {
          wipe?.remove();
          blackout?.remove();
          overlay?.remove();
          document.body.style.overflow = '';
          document.body.style.position = '';
        }, 400);
      }, 700); 
    }, 2000); 
  }

  window.addEventListener('load', runIntro);
</script>
