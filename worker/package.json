{
  "name": "pngu-auth-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Decap CMS GitHub OAuth backend for pngu.info.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.21.0"
  }
}
